spring:
  datasource:
    url: jdbc:h2:mem:testdbt
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
  sql:
    init:
      mode: always
      schema-locations: schema.sql
      data-locations: testdata.sql